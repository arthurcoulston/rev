# Claude Code shim

The canonical apparatus for this repository is **`AGENTS.md`** — read it first;
it routes every session (summoned roles load only their role file; dev sessions
load DEV.md). This file exists only because Claude Code looks for CLAUDE.md.
Substance never lives here.
